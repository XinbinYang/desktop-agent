const electron = require('electron');
console.log('electron module keys:', Object.keys(electron));
console.log('app:', typeof electron.app, electron.app);
console.log('typeof electron:', typeof electron);
